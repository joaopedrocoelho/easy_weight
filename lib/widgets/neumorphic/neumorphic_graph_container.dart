import 'package:flutter/material.dart';

class NeuGraphContainer extends StatelessWidget {
  const NeuGraphContainer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}